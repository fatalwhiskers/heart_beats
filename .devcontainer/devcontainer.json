{
  "name": "rPPG Heart Rate Pipeline",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {}
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  },

  "postCreateCommand": "apt-get update && apt-get install -y ffmpeg libsm6 libxext6 && pip install --upgrade pip && pip install -r requirements.txt",

  "forwardPorts": []
}
